/** Tile animation duration (in ms). */
export const animationDuration = 100;

/** Default board size. */
export const defaultBoardSize = 3;

/** A list of supported board sizes. */
export const supportedBoardSizes = [3, 4, 5, 6];

/** Tile value of the victorious tile. With circular merge system, victory is disabled. */
export const victoryTileValue = 0; // 0 means no victory condition

/** Grid gap (used for animations). */
export const gridGap = "1rem";
